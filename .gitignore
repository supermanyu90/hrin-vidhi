.venv/
__pycache__/
*.pyc
.env
.pytest_cache/
.ruff_cache/
.vercel/
fixtures/audio/*/
# uv resolves its own lock on Vercel, with an older uv than most machines run.
# A lock written by a newer uv can use a revision that one cannot read.
uv.lock
